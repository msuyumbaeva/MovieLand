<script type="text/javascript">
    var Urls = Urls || {};

    Urls.RelativeRoot = '@Url.Content("~")'

    Urls.Genre = {
        GetAll: '@Url.Action("LoadTable", "Genre")',
		Edit: '@Url.Action("Edit", "Genre")'
    }

    Urls.Country = {
        GetAll: '@Url.Action("LoadTable", "Country")',
		Edit: '@Url.Action("Edit", "Country")'
    }

    Urls.Artist = {
        GetAll: '@Url.Action("LoadTable", "Artist")',
		Edit: '@Url.Action("Edit", "Artist")'
    }

    Urls.Movie = {
        GetAll: '@Url.Action("LoadTable", "Movie")',
        Details: '@Url.Action("Details", "Movie")',
        Edit: '@Url.Action("Edit","Movie")',
        GetGenres: '@Url.Action("GetGenres", "Movie")',
        GetCountries: '@Url.Action("GetCountries", "Movie")',
        GetArtists: '@Url.Action("GetArtists", "Movie")',
        DeleteGenre: '@Url.Action("DeleteGenre", "Movie")',
        DeleteCountry: '@Url.Action("DeleteCountry", "Movie")',
        DeleteArtist: '@Url.Action("DeleteArtist", "Movie")'
    }

    Urls.Comment = {
        Create: '@Url.Action("Create", "Comment")',
        GetAll: '@Url.Action("LoadTable", "Comment")'
    }

</script>
